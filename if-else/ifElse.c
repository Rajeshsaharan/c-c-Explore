#include <stdio.h>

main(){
 //if or else statment can be use to check trythy or falsy value
 // we use relational operator to compare two expression / literal / variable or something
 // true ---> gives  1
 // false ----> give  0
 // both side of relational operator should be same otherwise use typecast to exchange data types 
 // never put ; end of any if else statment put this on every statmenr of if and else block
 if (3 > 2){
     printf("yes you are right");
 }else{
     printf("you are wrong");
 }
 // we can also use logical operator like && || or ! in if else statement

}